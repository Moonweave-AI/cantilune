---
title: Cantilune Core Theory closure delivery
document_type: technical-delivery
status: proved / review-pending
risk: S2
quality_target: QA-L4
maturity: M2 / FCP-ready candidate
owner: Joker-of-Gotham
dri: Joker-of-Gotham
updated: 2026-07-28
---

# Delivery outcome

This package delivers the ratified Cantilune **generic Core Theory plus a
nonempty substantive reference execution package** for immutable proof
binding and independent QA-L4 review.

Evidence binding:

| Field | Value |
|---|---|
| Verified source commit | `59a1a6885ef6a2774b2731f487f83228e67d15dc` |
| Evidence commit | `ed26cb74c4425b0d3025521f939695fd3fb8dee5` |
| Pointer / manifest commit | This document's committed revision; reported externally |
| Complete build/audit record | `docs/qa/evidence/2026-07-28-cantilune-theory-source-59a1a688.md` |
| Build-evidence SHA-256 | `36f1335e148a0d458f1bdfbd3d7eacf27cfd45bcd0ea2fb60a6b404b5ddc51b8` |
| Branch | `codex/theory-foundation` |
| Published cleanup head | `9375a2edafd5a7d7574a6a3d527f806122292051` |
| Review vehicle | [PR #1](https://github.com/Moonweave-AI/cantilune/pull/1) |
| Owner / DRI | Joker-of-Gotham / Joker-of-Gotham |
| Human review | Pending |
| FCP result | Pending |
| ADR status | Proposed |

The technical evidence values are now real and consistent: the source commit,
single-parent evidence commit, evidence record and hash are bound; CENTRAL-18
is complete; and the strict proved/tree gate passes at the later pointer
commit. The technical state is therefore **`proved / review-pending`**. It
does not become reviewed, FCP Passed, QA-L4 complete, or ADR Accepted without
the corresponding human records.

The published cleanup head is a descendant of pointer P. It removes generated
and temporary publication artifacts; it does not replace the proof identity
S/E/P. Subsequent README and review-metadata commits are documentation-only.
The live PR-head check, rather than a prose snapshot, is authoritative for
remote CI status.

# What the delivery contains

## Generic categorical and rewriting core

- FreeSMC equality quotient, category and symmetric monoidal structure, and
  the stated universal comparison/uniqueness theorem;
- finite typed positional open hypergraphs related to their exact
  adhesive-slice image;
- DPOI result/complement uniqueness and parallel-independent concurrency in
  the declared legal monic scope;
- signature extension coherence, observable quotient rewriting, deterministic
  event replay, path lift/reflection and terminal preservation;
- a parameterised total theorem for any package carrying the complete
  certificates.

## P1a

- `CompleteProductP1aProjectionScope`, which exposes a conforming product's
  supplied whole-LTS DAG, Petri, and morphism `ProjectionCertificate`s with
  full path lift/reflection and success/wait/deadlock preservation;
- `P1aSemanticCertificate` fields embedded in every complete
  `CoreConformancePackage`, tied to the package's same selected candidate and
  replayed `DPOEvent`;
- canonical configuration dependency graphs and total SCC condensation,
  including original-edge coverage, condensation acyclicity, and strict edge
  rank;
- an ordered singleton Petri declaration tied to the event's signature
  version/rule id, canonical individual-token/provenance markings, exact
  endpoint delta, enabling/firing, and retained-token identity;
- a separate fixed-signature fourteen-event business reference whose DAG,
  Petri, and morphism targets have independently declared carriers and native
  transitions, with exact source `DPOEvent` replay.

The SCC-DAG and provenance-Petri records are selected-occurrence semantic
sidecars, not whole-LTS projection certificates. The fourteen-event reference
is an independent anti-vacuity witness; it is not identified with the
substantive reconnect occurrence or with an arbitrary product package.

## Open-pi, P1b and P1c

- typed and polarised abstract boundary positions;
- a presented algebraic wiring SMC with tensor, wire, plug, hide and
  restriction laws;
- fresh nominal operational realisation, with three pairwise-fresh names for
  each singleton wire and globally disjoint canonical tensor blocks;
- process structure modulo alpha conversion and lawful ACU/scope laws;
- joint derivative-alpha treatment of bound actions and derivatives;
- native strong-step representatives and an explicit algebraic/native
  compatibility boundary;
- P1b request/accept structural late residual and reflection proof surface;
- fifteen normative native P1c event families;
- a total sixty-operation registry with `refinesTo` and enriched version,
  rule, session, correlation and occurrence metadata;
- one genuine strong late-pi step per normative event;
- explicit dynamic-admission phases: visible input to
  `admissionEstablished`, then a genuine tau `admissionReconnect` step, with
  the first actual-Agent target literally equal to the next reconnect source.

## Maximum-compatible D1-A FMS

The central proof object is named `MaximumCompatibleD1AFMSClosure`, with
constructor theorem `maximum_compatible_d1a_fms_closure`. It is intentionally
a single typed **record of compatible evidence branches**, not a claim that
the separated source-oriented construction and the non-separated D1-A
construction form one source-paper FMS model.

The delivery implements the ratified maximum-compatible D1-A route:

- an all-object lower omega-Scott closed-set effect on the stated omega-CPO
  category;
- Monad, strength, chosen symmetric Fubini and multiplication coherence;
- one effect bottom, with effect-level divergence equal to deadlock;
- an actual continuous-natural recursive agent-domain solution for the
  selected functor, with its stated fold/unfold properties;
- action restriction and hiding on that concrete solution;
- an operational bridge for native events, including a substantive reconnect;
- finite bottom/Hoare native-trace adequacy and full abstraction, plus
  realization of explicitly supplied finitely generated trace sets;
- `RecursiveProc` guarded finite-action-trace/contextual-Hoare
  adequacy/full abstraction, not an actual-Agent theorem;
- contextual completion over the declared process constructors, context
  congruence and `contextualSourceInterpretation` from source processes to
  their constructed semantic values;
- actual-Agent native-path adequacy/full abstraction for deterministic typed
  tau/free-output prefix tries and constructive realization of the
  syntax-defined `CompactPrefixPoint`s; the name does not establish that all
  order-theoretic compact Agent elements are denotable;
- a total supported finite-control coalgebra and exact actual-Agent
  commutation for all fifteen compiled normative families.

This is not described as the separated Abramsky source powerdomain. Its full
abstraction is for the expressly stated D1-A bottom/Hoare contextual
observation, not constructor-sensitive strong bisimulation. The wider
guarded/contextual theorem is not an unrestricted actual-Agent theorem, and
`contextualSourceInterpretation` is not reverse definability. The distinct
separated branch supplies the all-source solution-set/enriched adjunction but
not commutative canonical Fubini; its universal property is not imported into
D1-A.

## Feedback, probability and total reference

- finite-height evidence closure and forward-invariant stable set;
- strict internal rank/no internal oscillation;
- conditional almost-sure stable-set hitting and the stated expected hitting
  bound under fairness, signature stability and positive epsilon;
- event labels, epoch alignment and exact replay in the shared trajectory;
- explicit success, external wait, deadlock and productive-infinite
  classifications;
- a substantive reconnect reference occurrence carried through the four
  projections and exact evidence bridge;
- `CompleteProductCommonTrajectoryCertificate`, which binds the same
  `OperationId`/registry family, canonical metadata, payload, fixed-epoch
  candidate, native/replay/raw derivations, selected-event
  `TrajectoryAgreement`, and actual-Agent endpoints in one record;
- a no-argument substantive `.instanceReconnect` closure and a separate
  admission-to-reconnect certificate with a literal heterogeneous
  actual-Agent endpoint seam.

The complete CENTRAL-18 implementation is bound to the immutable source
commit, the resumed clean-build/audit record, and the proved manifest. It is
`proved / review-pending`. Unrelated existentials do not discharge the generic
certificate.

# Proof boundaries fixed by kernel no-go results

The following rejected shortcuts are part of the delivery:

1. D1-A cannot distinguish exposed divergence/deadlock constants in its effect
   equality and still be fully abstract for that distinction.
2. D1-A bottom/choice semantics cannot be fully abstract for
   constructor-sensitive strong bisimulation, even on the finite tau/choice
   witness.
3. No single pi-process syntax can define every element of every omega-CPO.
4. A fixed positive-prefix finite raw relay is not a structural categorical
   identity, and no fixed finite-control forwarder supports unbounded native
   reuse.

The positive construction follows the maximum compatible route:

- bottom/Hoare contextual denotation plus independent native strong-step
  theorems;
- finite Hoare finitely-generated definability and explicit
  `CompactPrefixPoint` realization, with no reverse semantic-image claim;
- presented algebraic wiring plus native operational commutation;
- explicit operational terminal separation above the unseparated effect.

No target was weakened silently.

# Product Conformance boundary

The eight planned production packages are intentionally absent from this Core
Theory delivery. The generic theorem applies only after a package supplies
real, checked witnesses for:

- its rule inventory and per-rule four-projection maps;
- cross-signature admission and replay;
- DAG rank/SCC facts and Petri pre-net/token facts;
- resources, live sessions and quiescent deletion;
- authorization and external evidence;
- fairness, stable signature window and positive epsilon;
- its actual production execution/Markov kernel and trajectory agreement.

No generic theorem or package name manufactures those facts. Each package
requires a separate Product Conformance record and review.

# Canonical artifact map

| Artifact | Purpose |
|---|---|
| `formal/Cantilune.lean` | Root proof import |
| `formal/Cantilune/Tests/All.lean` | Root regression import |
| `formal/proof-obligations.json` | Central theorem inventory and evidence status |
| `formal/source-integrity.json` | Source and pinned-input hashes |
| `formal/axiom-audit-targets.txt` | Maintained kernel-assumption surface |
| `formal/scripts/ci.ps1` | Development and proved/review-pending gate |
| `formal/scripts/recompute-integrity.ps1` | Integrity regeneration |
| `docs/rfc/0002-projection-consistency.md` | Ratified architecture and compatibility boundary |
| `docs/adr/0001-unified-formal-structure.md` | Proposed architecture decision |
| `docs/qa/0002-theory-closure-proved-review-pending-2026-07-27.md` | QA-L4 review packet |
| `docs/governance/reviewer-assignments.md` | Reviewer ownership, COI, and recruitment gate |
| `docs/research/0027-final-load-bearing-seams-2026-07-27.md` | Final P1a/admission/common-trajectory seam record |

Historical reports that claimed completion without an actual kernel build are
not controlling evidence.

# Reproduction and verification

The pinned build began with `-CleanBuild`, was interrupted only by the external
runner limit, and resumed after a stable process-drain check. The evidence
commit introduces the hash-bound composite record; this later pointer commit
binds the manifest metadata. The strict proved/tree gate verifies that every
proof-sensitive path is byte-identical to the verified source commit. The
recorded verification commands are:

```powershell
.\formal\scripts\ci.ps1 -CleanBuild        # initial source-bound run
.\formal\scripts\ci.ps1                    # process-drained continuation
.\formal\scripts\ci.ps1 -RequireProved -VerifyTreeOnly  # at pointer commit
```

The strict gate must confirm:

- no untracked proof-sensitive files;
- source-integrity and pinned-input hashes match;
- root builds succeed;
- zero whole-word `sorry`, `admit`, `axiom`, or `unsafe`;
- every axiom-audit target resolves exactly once and uses only the documented
  foundations;
- every accepted central obligation is `proved` and points to the same
  verified source commit and build record;
- CENTRAL-18 contains the operation/registry/metadata/payload/admission/
  trajectory common chain, rather than merely adjacent product and FMS facts.

The controlling result is `docs/qa/evidence/2026-07-28-cantilune-theory-source-59a1a688.md`; this document does not
substitute for actually running the commands.

# Review and governance disposition

| Gate | Current disposition |
|---|---|
| Kernel proof and reproducibility | Proved; independent review pending |
| Category/DPO/Petri independent review | Pending |
| Pi/domain independent review | Pending |
| Lean/provenance independent review | Pending |
| Owner/DRI receipt | Pending |
| RFC-0002 FCP | Not passed / pending real process |
| ADR-0001 | Proposed |
| Eight production packages | Deferred Product Conformance |

The exact handoff statement is:

> Cantilune generic Core Theory and its substantive reference execution
> package are proved by the pinned Lean kernel at the recorded source commit;
> independent QA-L4 review and human governance acceptance remain pending.

No stronger status is authorised by this delivery.

Owner receipt, reviewer signatures, FCP disposition, and ADR acceptance must
be recorded in an authoritative Issue, PR, or governance record. This delivery
document states what is ready to receive; it is not itself a human signature.
