# FCP entry and Owner review disposition (2026-08-16)

| Field | Value |
| --- | --- |
| Status | **FCP open** (not FCP-closed / not RFC Accepted) |
| Opened | 2026-08-16 |
| Comment period | 14 days (IETF RFC 2026 Last-Call minimum for WG-originated actions) |
| Scheduled close | 2026-08-30 |
| Decision Owner | Joker-of-Gotham |
| COI | Owner is also DRI, Architecture, Security, Formal, Process, Lean Assumptions, QA-L5 lead, and AI-Eval reviewer |

## Owner decisions recorded in chat (2026-08-16, morning)

1. Enter FCP. Owner personally signs the formal governance review. COI disclosed.
2. Authorize public evaluation claims. AI-Eval reviewer = Joker-of-Gotham (Owner; COI).
3. Owner signs the QA-L5 checklist (COI). Independence rows that forbid self-review are **waived**, not pretended to be independent.
4. Keep SemVer **0.x** (semver.org §4: public API is not stable). FCP ≠ 1.0.0.
5. Start soak immediately (later the same day: 8h is sufficient).
6. Keep Windows Home Hyper-V fail-closed (G17). Linux isolation remains WSL `runsc`.
7. After the authorized soak, commit the engineering tree plus the sanitized soak summary together. No commit until the Owner orders one.
8. Accept ADR-0001 with Owner COI (2026-08-16). Not Lean `reviewed`. Not FCP-closed.
9. Do **not** issue any public superiority `ClaimDecision` in this window. Authorization framework only.
10. Publish npm **0.x** after the authorized soak is green; do not wait for 2026-08-30 FCP close. Still no stable API. Needs `NPM_TOKEN` + GitHub Release at that time.

## Owner decisions recorded later the same day (2026-08-16)

1. **Lean: accepted.** Kernel rows stay `proved` (`requiredGate` unchanged; promotion form unused). Owner Accept is recorded in `formal/proof-obligations.json` `ownerAccept`. Living docs say `proved / Owner-accepted`.
2. **SS-01 lifted.** `@cantilune/conformance` **is** production release authority for this 0.x engineering release.
3. **OTel:** Cantilune OTLP export is production. Official `gen_ai.*` keys remain Development (upstream).
4. **HTTP MCP is allowed.** Hot-attach still requires schema admission that advances the epoch; the current turn keeps the old tool surface.
5. **Soak is 8h.** Run `2026-08-15T19-04-39-535Z` cut at the 8h mark: 1881 cycles, 0 failures. Later unattended soaks default to 8h. PR CI stays shortened.
6. **Multi-host durable:** operator Postgres HA **or** official etcd Raft (ADR-0029).

## Hard limits that remain (do not conflate with “1.0 / FCP-closed”)

| Limit | Record |
| --- | --- |
| SemVer 0.x; no stable API / schema | `pnpm check:0x` in `test:static` |
| Lean kernel rows `proved`; Owner-accepted; not `reviewed` | promotion form unused; `ownerAccept` recorded |
| FCP open ≠ FCP closed / RFC Accepted | closes 2026-08-30 |
| Evaluation analysis ≠ `supported` | only attested `ClaimDecision` after Owner COI quorum |
| Windows Home Hyper-V | G17 fail-closed; isolation is WSL `runsc` |
| Default sandbox image | `alpine:3.20` pulled in `Ubuntu-24.04` (2026-08-16); `docker run --runtime=runsc alpine:3.20 echo sandbox-ok` |
| 8h soak | script writes `.cantilune/soak/<runId>/`; PR CI stays shortened |
| Multi-host durable | operator Postgres HA or official etcd Raft (ADR-0029) |

## Waiver rationale (A-06, E-03, F-06, SS-04, SS-09)

The QA-L5 checklist requires reviewers distinct from the implementer DRI. No second human is available. Owner waived the independence requirement for this FCP window, with COI recorded here and in `reviewer-assignments.md`. This is a disclosed self-review, not an external audit.

## Signatures

**Decision Owner / Formal / Process / Lean Assumptions / QA-L5 / AI-Eval / Architecture / Security:** Joker-of-Gotham  
**Date:** 2026-08-16  
**COI:** disclosed
