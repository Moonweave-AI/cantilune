# DRI-ratified theory execution boundary — 2026-07-27

## Classification

- Work object: formal-semantics research and implementation program.
- Risk: S2, because an incorrect proof claim changes a load-bearing
  architecture decision but does not directly operate a production system.
- Quality target: QA-L4.
- Maturity at entry: M1 / Pre-FCP.
- Owner and Decision Owner: Joker-of-Gotham.
- Authoritative decisions: RFC-0002 section 25 and the status correction
  appended to ADR-0001.

## Ratified scope

The DRI authorised autonomous completion of the parameterised Cantilune Core
Theory and its nonempty reference execution packages. The eight production
packages remain separate Product Conformance work because their rule
inventories and runtime facts are absent. The generic theorem must quantify
over complete package certificates; it must not manufacture those
certificates.

The ratified semantic route is:

- unseparated bottom in the commutative FMS effect layer;
- operational and terminal separation of divergence and deadlock;
- complete kernel-built FMS as a P1 gate;
- finite-process full abstraction followed by arbitrary guarded
  recursion/replication;
- typed polarised positional Open-pi boundaries with fresh nominal
  realisation;
- structural and derivative-alpha quotients, not bisimulation equality;
- fifteen native P1c event families and a sixty-operation refinement registry;
- one strong native late-pi step per normative source phase event;
- SCC-condensed total DAG plus strict rankable subview;
- individual-token Petri provenance; and
- kernel no-go results rather than silent weakening when requirements conflict.

All-domain-element definability is a best-effort strengthening beyond the
source FMS process theorem. It cannot be assumed as a field of the package used
to “prove” itself.

## Authority and prohibitions

The DRI authorised edits to formal sources, tests, RFC, ADR, proof manifest,
QA evidence, and research logs; local build and audit commands; read-only
primary-source research; and history-preserving local commits on
`codex/theory-foundation`. Push and PR creation remain unauthorised.

The implementation may not fabricate:

- an eight-package Product Conformance result;
- a native target derivation;
- an external theorem's Lean proof;
- an independent reviewer or human signature;
- FCP passage, QA-L4 completion, or ADR acceptance.

The maximum pre-signature governance state is
`proved / review-pending`.

## Reproducible entry baseline

The repository development evidence gate was actually executed from the
repository root with:

```powershell
$env:PATH = "$env:USERPROFILE\.elan\bin;$env:PATH"
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass -Force
.\formal\scripts\ci.ps1
```

Observed result:

- pinned Lean `4.32.0` and Lake `5.0.0-src`;
- 485 Lean project source files;
- aggregate source hash
  `c21eb3dccf06d3c431d49592d6a0ef433cc5fc474b571d0863f6db176445a1f4`;
- zero whole-word `sorry`, `admit`, project `axiom`, or `unsafe`;
- 9,139 successful build jobs;
- axiom audit restricted to `propext`, `Classical.choice`, and `Quot.sound`;
- ordinary development gate exit code 0.

This is evidence for the mutable worktree only. It is not immutable commit
evidence and does not establish that the manifest's theorem statements have
the required scope.

## Immediate research question

Determine whether the existing FMS files contain concrete inhabitants with the
required operational quantifiers, rather than interfaces whose hypotheses are
the desired result. Then construct the missing inhabitants, integrate the
positional Open-pi operational bridge, and promote each central obligation
only after statement-level scope review and commit-bound verification.
