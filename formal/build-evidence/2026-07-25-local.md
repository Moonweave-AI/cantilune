# Local formal build evidence — 2026-07-25

Status: successful local kernel build of the working tree; uncommitted and
not independently reviewed.

Repository `HEAD` at execution time:
`078da5f19a14538032b2b139600eef9ec9e49711`.  The proof-sensitive `formal/`
tree is untracked/modified relative to that commit, so this record is not
immutable commit-bound evidence and does not promote any manifest entry to
`proved` or `reviewed`.

Executed from `formal/`:

```powershell
$env:PATH="$env:USERPROFILE\.elan\bin;$env:PATH"
powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\ci.ps1
```

Observed result:

- pinned toolchain: `leanprover/lean4:v4.32.0`;
- Lean: `4.32.0`, commit `8c9756b28d64dab099da31a4c09229a9e6a2ef35`;
- source-integrity gate: 234 Lean files, aggregate
  `282869e3bae154431bd49e612fd34183350f81978f70d042af8a981b3f3574a2`;
- whole-word placeholder gate: zero `sorry`, `admit`, `axiom`, or `unsafe`;
- full `lake build`: successful, 8889 jobs;
- kernel dependency audit: 487 declarations parsed exactly once;
- dependency allowlist: only `propext`, `Classical.choice`, and `Quot.sound`;
- proof manifest: 18 approved central obligation names and valid statuses.

The successful build includes the exact-positional finite DPOI bridge, native
typed late-pi/P1c matrix, replayable event/epoch probability bridges, finite
strict omega-CPO fragment, shared fourteen-event P1a business certificates,
and concrete P1c terminal/productive classification.

It does not supply the missing complete FMS powerdomain/domain/full-abstraction
inhabitant, exact P1b structural residual reflection, arbitrary product
projection/resource inhabitants, product scheduler premises, immutable
provenance, or independent QA-L4/FCP/ADR approval.
