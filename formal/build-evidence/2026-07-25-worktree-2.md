# Local formal build evidence — 2026-07-25 (worktree iteration 2)

Status: successful local kernel build of the working tree; uncommitted and
not independently reviewed.

This record supersedes the earlier `2026-07-25-local.md` only insofar as that
record's aggregate (234 files) no longer matches the current worktree (241
files). Both are uncommitted local evidence; neither promotes any manifest
entry to `proved` or `reviewed`.

Repository `HEAD` at execution time:
`078da5f19a14538032b2b139600eef9ec9e49711`.  The proof-sensitive `formal/`
tree is untracked/modified relative to that commit, so this record is not
immutable commit-bound evidence and does not promote any manifest entry.

## Changes since `2026-07-25-local.md`

1. **P1b reflection scaffolding (kernel-checked).** Added
   `Cantilune.Pi.P1bStructuralLateBridge.step_decompose` (every
   `Late.structuralLateLTS.ObservableStep` decomposes into source congruence
   + native step + target congruence) and `complete_reflect` (no `Late.Step`
   leaves the complete state through any structurally congruent
   representative). These are verified helpers toward the open
   `StandardLateReflection` obligation; they do not inhabit it. The
   `requesting`/`established` structural-residual transport remains open.
2. **Build restoration.** Corrected two worktree defects that had prevented
   a clean full build:
   - `Cantilune/Pi/P1cAdmittedP1aCertificates.lean` referenced `Core.Package`
     types without importing `Cantilune.Core.Package`; the import is added.
   - `Cantilune/Tests/P1cAdmittedP1aCertificates.lean` used unqualified
     `P1cAdmittedOperations.DAG.Step` identifiers; these are now fully
     qualified as `Cantilune.Pi.P1cAdmittedOperations.*`.
3. **Evidence-gate repairs.**
   - Reworded a doc comment in
     `Cantilune/Feedback/AuthorizedFeedbackExecution.lean` that used the
     English verb "admit" so it no longer trips the forbidden-placeholder
     whole-word gate. The gate's `sorry`/`admit`/`axiom`/`unsafe` security
     intent is unchanged.
   - `scripts/ci.ps1` now decodes `lake env lean` axiom-audit output as
     UTF-8 before regex parsing, so non-ASCII declaration names such as
     `finite_chain_reaches_ωSup` round-trip correctly. The allowlist
     (`propext`, `Classical.choice`, `Quot.sound`) is unchanged; this only
     makes the existing audit parse those names instead of failing to match
     them.

## Executed from `formal/`

```powershell
$env:PATH="$env:USERPROFILE\.elan\bin;$env:PATH"
powershell -NoProfile -Command "Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass -Force; & ./scripts/ci.ps1"
```

## Observed result

- pinned toolchain: `leanprover/lean4:v4.32.0`;
- Lean: `4.32.0`, commit `8c9756b28d64dab099da31a4c09229a9a6a2ef35`;
- source-integrity gate: 241 Lean files, aggregate
  `8f36587fdbd23db27f251cd33c0a1e8d699a56ee48f97b4fad904e42817b5bef`;
- whole-word placeholder gate: zero `sorry`, `admit`, `axiom`, or `unsafe`;
- full `lake build`: successful, 8894 jobs;
- kernel dependency audit: 487 declarations parsed exactly once;
- dependency allowlist: only `propext`, `Classical.choice`, and `Quot.sound`;
- proof manifest: 18 approved central obligation names and valid statuses.

## Boundary

This evidence is local and uncommitted. It is not immutable provenance,
independent QA-L4 review, FCP approval, or ADR acceptance. No central
obligation is promoted. The full FMS powerdomain/domain/full-abstraction
inhabitant, the exact P1b structural-residual reflection, the arbitrary
product projection/resource inhabitants, the product scheduler premises,
and the independent QA-L4/FCP/ADR gate remain open.
