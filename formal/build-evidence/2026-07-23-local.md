# Local formal build evidence — 2026-07-23

Status: successful local kernel build; uncommitted; scoped independent Agent
reviews complete; human QA-L4 review pending.

This record is durable command evidence for the current working tree. It is
not a QA-L4 approval, an FCP decision, or a substitute for an immutable source
commit.

## Source identity

- Git baseline HEAD:
  `a592d868f19556361cb52aa03772912af4e8bed4`
- Working branch: `codex/theory-foundation`
- Project-owned Lean source files: 73
- Aggregate SHA-256 of sorted
  `relative/path SHA256(file-content)` entries for project-owned `*.lean`
  files:
  `90461f78d778d85d6db66ef2b0048985f9ab80087e3fbd971bfc2c52235a1420`
- `formal/source-integrity.json` makes the source count, aggregate, toolchain,
  Lake inputs, dependency lock, and maintained axiom target set executable CI
  checks rather than prose-only claims.
- The working tree contains uncommitted and untracked implementation files.
  Therefore no proof-obligation entry is promoted to `proved`.

## Pinned environment

- Lean toolchain: `leanprover/lean4:v4.32.0`
- Lean commit: `8c9756b28d64dab099da31a4c09229a9e6a2ef35`
- Lake: `5.0.0-src+8c9756b`
- mathlib tag: `v4.32.0`
- resolved mathlib commit:
  `81a5d257c8e410db227a6665ed08f64fea08e997`
- `lake-manifest.json` SHA-256:
  `8b56c79e5da2637f74afb898ed08c8efda78892d5065ff07f822f0dba62b8170`

## Executed gate

Command, from the repository root:

```powershell
$env:Path = 'C:\Users\NJHL\.elan\bin;' + $env:Path
.\formal\scripts\ci.ps1
```

Observed result:

- exit code `0`;
- source-integrity gate matched 73 files and aggregate
  `90461f78d778d85d6db66ef2b0048985f9ab80087e3fbd971bfc2c52235a1420`;
- placeholder gate clean across 73 project-owned Lean sources;
- `lake build` completed successfully with 8,728 jobs;
- the finite regression suite was included through the root import;
- 88 maintained declarations produced exactly one parsed kernel dependency
  report each, all accepted by the explicit allowlist.

The printed dependencies were limited to Lean/mathlib foundational principles
already expected by the project: `propext`, `Classical.choice`, and
`Quot.sound`; several results used fewer or none. No project-local unchecked
principle was introduced.

The expected-failure promotion check was re-executed:

```powershell
.\formal\scripts\ci.ps1 -RequireComplete
```

It returned exit code `1` and correctly rejected the manifest before
rebuilding because all 18 entries are `partial_scaffold`.

## Subsequent targeted probability verification

After the finite-cylinder trajectory/matrix identification was internalized,
the following targeted command was executed from `formal/`:

```powershell
& 'C:\Users\NJHL\AppData\Local\lean-toolchains\lean-4.32.0-windows-sha98acc2f9\bin\lake.exe' build Cantilune.Tests.StochasticExecution
```

Observed result:

- exit code `0`;
- `Cantilune.Feedback.StochasticExecution` and
  `Cantilune.Tests.StochasticExecution` completed successfully, with
  8,664 jobs;
- the nontrivial finite Boolean kernel checks a first-step not-hit probability
  of `1 / 2`, the corresponding real miss probability, and almost-sure
  hitting without any caller-supplied state-trajectory-agreement premise;
- `#print axioms` for
  `trajectory_notHit_eq_missProbability` and
  `finite_kernel_feedback_almost_sure_hitting` reported only `propext`,
  `Classical.choice`, and `Quot.sound`.

This targeted run supplements rather than replaces the full CI/source-
integrity snapshot above. The working tree remains uncommitted and
without human QA-L4 review; no manifest status is promoted by this evidence.

## Integrated DPOI/FMS/P1c verification

The final full gate additionally covered:

- `FinitePresheafDPOI`: the fixed-host active-view inclusion functor,
  pointwise union/intersection pushouts, both `InterfaceLocal` typed-slice DPO
  squares, complement encoding uniqueness, and its regression fixture;
- `FMSFinitePower`, `FMSContext`, and `FMSFiniteAgent`: the actual `Finset`
  monad, pointwise `Type^I` monad, locally nameless process functor, natural
  support denotation, ACUI quotient, and finite object-level approximants;
- `FMSExternalPackage`: an uninhabited conditional boundary whose operational
  relation now includes freshness and universal substitution after late input;
- `P1cMatrix.no_complete_extension_preserving_pi`: completion of other
  projection columns cannot preserve the current incomplete π column; and
- `trajectory_notHit_eq_missProbability`: the finite native-supported state
  kernel's Ionescu--Tulcea not-hit probability equals its killed-chain miss
  mass without an agreement premise.

Two independent read-only Agent reviews accepted the scoped DPOI and finite
state-probability results and requested changes for stronger claims. A third
review confirmed the real Set-side FMS constructions but found that the
external package has no inhabitant and is itself still missing world/action,
`res`, and strong-commutative-monad coherence required to certify FMS. These
Agent reviews are technical evidence only, not the required human QA-L4
signatures.

The final integrated build was warning-free. The reducible Prop-valued
inclusion/pushout witnesses in `FinitePresheafDPOI.lean` have a documented
file-local linter policy because later pointwise proofs compute through them.

## Proof-surface result

All eighteen central manifest names denote kernel-buildable supporting
declarations and are classified as `partial_scaffold`. None of the central
obligations is classified as `proved` or `reviewed`.

The implemented names do not erase their documented boundaries:

- the generated FreeSMC equation quotient, full internal coherence law bundle,
  strict identity-on-object universal property, and corresponding coherent
  monoidal-isomorphism form build; arbitrary object-mapping mathlib
  `MonoidalCategory` targets remain outside that result;
- finite typed-open inclusion-match DPOI constructs complements/results and
  proves subject reduction/carrier concurrency. A fixed-host active-view
  inclusion category now maps faithfully to the typed presheaf slice, and
  `InterfaceLocal` events yield two genuine DPO pushouts. Ordered-port
  fidelity, a full/equivalence bridge, arbitrary-monic complement existence,
  and categorical concurrency remain open;
- general operational and shared five-layer P1a combinators build, but the
  concrete DAG and pre-net values remain two-event fixtures and do not supply
  intended static/DPO/cross-layer instances;
- finite closed request/accept and one-delegation certificates observe every
  native target action. Finite-control alpha equivalence, structural
  congruence, capture-avoiding substitution, and freshness-guarded strong
  late steps now build, but the typed kernel has weaker freshness premises;
- append-only four-view admission has finite executable instances, and one
  admission event maps to an unfiltered native π input; this is not a general
  all-projection admission theorem;
- deterministic replay uses endpoint-free recipes and a verified function
  kernel; concrete general DPO rule kernels remain absent;
- one node-only deletion fixture enforces explicit dangling, resource,
  session, and tombstone obligations but is not the M-adhesive DPO theorem;
- the explicit P1c matrix has 60 typed cells: 12 native strong π cells and 48
  pending cells. The current mismatch/reconnect/quiescent-delete derivation
  types are proved uninhabited, so completion requires an RFC/ADR decision;
- one shared finite source has a four-target operational witness, while the
  central five-layer four-projection theorem only combines four already
  supplied complete certificates; no intended-calculus four-certificate
  instance exists;
- actual nonconstant `World ⥤ Type` and `World ⥤ ωCPO` support functors,
  the genuine finite-power and pointwise `Type^I` monads, locally nameless
  supported-process natural support, finite object-level `P_f(H-)` stages,
  omega-continuous support action, and a concrete pointwise OpenPi
  support-model commuting theorem build. This is not the FMS Set initial
  algebra or CPO powerdomain/domain equation. The uninhabited external package
  also lacks world/action, `res`, and strong-monad coherence required for
  acceptance; adequate hiding and full abstraction remain absent;
- probabilistic convergence includes a genuine Markov kernel,
  Ionescu--Tulcea trajectory measure, measurable not-hit events, and
  almost-sure hitting. For finite native-supported stochastic matrices the
  geometric recurrence is derived from row sums and a supplied pointwise
  epsilon bound, while a general finite-cylinder induction proves that
  killed-chain miss mass equals the Ionescu--Tulcea not-hit probability.
  This identifies state trajectories, not event-labelled replay: diagonal
  holding has no event obligation, off-diagonal support is existential, and
  kernel time is not yet package epoch time. Stable-region/window, fairness,
  and positive-epsilon progress witnesses remain package-specific;
- a deterministic `ExecutionPackage`-to-feedback bridge proves exact finite
  path replay and stability transfer, but does not supply that stochastic
  kernel.

These boundaries are why the total four-projection theorem, QA-L4, FCP, and
ADR acceptance remain open.
